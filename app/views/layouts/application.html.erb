<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Obs2" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Obs2" %>">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%# Modernizr is required for Zurb Foundation %>
    <%= javascript_include_tag 'vendor/modernizr' %>
    <%= javascript_include_tag :application %>
    <%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>
    <script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
    <script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>
    <%= csrf_meta_tags %>
  </head>
  <body>

      <div class="hero hide-for-print">
        <div class="row">
         <div class="main-wrapper">
            <div class="large-12 columns">

              <h1>Easy</h1>
              <h3>Astronomy Log</h3>
              <hr>
              <div class="row">
                <div class="large-8 columns large-centered">
                  <% if !user_signed_in? %>
                      <ul class="button-group even-3">
                  <% else %>
                      <ul class="button-group even-6">
                    <% end %>
                    <%= render 'layouts/navigation_links' %>
                  </ul>
                </div>
              </div>
            </div>
          </div>
        </div>
        <% if !user_signed_in? %>
          <div class="row">
            <div class="large-6 columns about-box">
              <div class="black-box">
                <h2>I wanted a great astronomy log.</h2>
                <p>But it was hard to find one that was easy and kept track of observing programs.  So I wrote one</p>
                <p>Want to learn more?</p>
                <a href="#" class="secondary small button">Learn More →</a>
              </div>
            </div>
          </div>

        <% end %>
     </div>

    <div class="hero-content">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </div>

     <div class="hide-for-print">
        <div class="row">
        <div class="large-12 columns">
          <div class="panel">
            <div class="row">
              <div class="large-9 columns">
                <h4>Get in touch!</h4>
                <p>We're open 9am - 5pm, Monday through Saturdays.</p>
              </div>
              <div class="large-3 columns">
                <a href="#" class="radius button right">Contact Us</a>
              </div>
            </div>
          </div>
        </div>
      </div>


        <footer >
          <div class="row">
            <div class="large-6 columns">
              <p>© Copyright no one at all. Go to town.</p>
            </div>
            <div class="large-6 columns">
              <ul class="inline-list right">
                <li><a href="#">About</a></li>
                <li><a href="#">Terms of Use</a></li>
                <li><a href="#">Privacy Policy</a></li>
              </ul>
            </div>
          </div>
        </footer>
      </div>
  </body>
</head>
</html>
